<% layout("layouts/boilerplate.ejs") %>
    <body>
        <!-- <h3>All Listings</h3> -->
        <!-- <form action="/listings/new" method="get">
        <button type="submit">Create New Listings</button>
    </form> -->
    <div id="filters">
        <div class="filter">
            <i class="fa-solid fa-fire"></i>
            <p>Trending</p>
        </div>
        <div class="filter">
            <i class="fa-solid fa-bed"></i>
            <p>Rooms</p>
        </div>
        <div class="filter">
            <i class="fa-solid fa-mountain-city"></i>
            <p>Iconic cities</p>
        </div>
        <div class="filter">
            <i class="fa-solid fa-mountain"></i>
            <p>Mountains</p>
        </div>
        <div class="filter">
            <i class="fa-brands fa-fort-awesome"></i>
            <p>castles</p>
        </div>
        <div class="filter">
            <i class="fa-solid fa-person-swimming"></i>
            <p>Amazing pools</p>
        </div>
        <div class="filter">
            <i class="fa-brands fa-fort-awesome"></i>
            <p>castles</p>
        </div>
        <div class="filter">
            <i class="fa-solid fa-campground"></i>
            <p>campings</p>
        </div>
        <div class="filter">
            <i class="fa-solid fa-cow"></i>
            <p>farms</p>
        </div>
        <div class="filter">
            <i class="fa-regular fa-snowflake"></i>
            <p>Arctic</p>
        </div>
        <div class="tax-toggle">
            <div class="form-check-reverse form-switch">
                <input class="form-check-input" type="checkbox" role="switch" id="flexSwitchCheckDefault">
                <label class="form-check-label" for="flexSwitchCheckDefault">Display total after taxes</label>
              </div>
        </div>
    </div>
        <div class="row gap-3 mt-3">
            <% for(let listing of allListings){ %>
                <div class="card allCard listing-card flex-basis-1 " style="width: 19rem;">
                        <a href="/listings/<%= listing._id %>">
                        <img src="<%= listing.image.url %>" class="card-img-top" alt="...">
                        <div class="overlay"></div>
                        <div class="card-body">
                            <p class="card-text">
                                <b><%= listing.title %></b><br>
                                    &#x20B9; <%= listing.price.toLocaleString("en-IN") %>/night
                                    <i class="tax-info">&nbsp;&nbsp;+18% GST</i>
                            </p>
                        </div>
                    </a>
                    </div>
                    <% } %>
        </div>
    </body>

    <script>

        let taxSwitch = document.getElementById("flexSwitchCheckDefault");
        taxSwitch.addEventListener("click", ()=>{
            console.log("clicked");
            let taxInfo = document.getElementsByClassName("tax-info")
            Array.from(taxInfo).forEach(info=>{
                if(info.style.display != "inline"){
                    info.style.display = "inline"
                }else{
                    info.style.display = "none"
                }
            })
        })

        let input = document.querySelector(".search-inp");
        let searchBtn = document.querySelector(".search-btn");
        let allCard = document.querySelectorAll(".allCard");
        searchBtn.addEventListener("click", (event)=>{
            event.preventDefault();
            for(let i = 0; i<= allCard.length; i++){
                let cardText = allCard[i].querySelector(".card-text b")
                let cardValue = cardText.innerHTML.toUpperCase();
                console.log(location)
                if(cardValue.indexOf(input.value.toUpperCase()) > -1){
                    console.log(allCard[i])
                    allCard[i].style.display = "";
                }
                else{
                    allCard[i].style.display = "none";
                }               
            }
          
        })
    </script>